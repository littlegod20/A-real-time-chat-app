import UserContainer from "./components/UserContainer";

const ChatPage = () => {
  return (
    <main className="flex flex-col items-start justify-center gap-3 py-2 w-full">
      <UserContainer />
    </main>
  );
};

export default ChatPage;
